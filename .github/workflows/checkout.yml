on:
  push:
    branches:
      - 'feature/**'
      - 'bugfix/**'
      - 'hotfix/**'
      - 'support/**'
      - 'master'
  pull_request:
    branches:
      - 'feature/**'
      - 'bugfix/**'
      - 'hotfix/**'
      - 'support/**'
  workflow_dispatch:

jobs:
  themegen:
    uses: blazeltd/themegen/.github/workflows/shared_checkout.yml@master
    with:
      directory: ./themegen
  
  themegen_annotation:
    uses: blazeltd/themegen/.github/workflows/shared_checkout.yml@master
    with:
      directory: ./themegen_annotation

        
      
    