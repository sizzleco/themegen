name: example
description: A new Flutter package project.
version: 0.0.1
homepage: "https://github.com"
publish_to: "none"

environment:
  sdk: '>=2.18.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  themegen_annotation:
    path: ../../themegen_annotation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  themegen:
    path: ../
  build_runner: ^2.2.1
  

flutter:
